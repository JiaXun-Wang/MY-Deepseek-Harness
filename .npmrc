# Default project-local pnpm store (pnpm v11 layout lives in .pnpm-store).
# No custom store-dir: the local virtual store already contains the packages
# fetched on the first run, so reinstall is nearly offline.
# Faster registry mirror (avoids timeouts on large optional binaries).
registry=https://registry.npmmirror.com
